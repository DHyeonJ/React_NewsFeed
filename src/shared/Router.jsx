import { BrowserRouter, Routes, Route } from 'react-router-dom';

const Router = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route />
        <Route path="/login" />
      </Routes>
    </BrowserRouter>
  );
};

export default Router;
